import ResetPasswordForm from "../components/Auth/reset-paswword";

const ResetPassword = () => {
  return (
    <div className="flex min-h-screen justify-center items-center">
      {/* Right side (Reset Password Form) */}
      <ResetPasswordForm />
    </div>
  );
};

export default ResetPassword;
